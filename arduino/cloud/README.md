# Arduino Cloud Artifacts

Store exported Arduino Cloud metadata here.

Recommended tracked files:
- `thing.template.json` exported from `scripts/cloud_export_thing_template.ps1`

Do not commit credentials files.
